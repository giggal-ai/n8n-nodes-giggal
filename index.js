// This file is intentionally empty. n8n loads the credentials and nodes
// from the paths declared in the "n8n" field of package.json (which point
// at the compiled files in dist/). This module is a no-op required only
// so `require('n8n-nodes-giggal')` succeeds in some environments.
module.exports = {};
